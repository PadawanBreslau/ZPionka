<h1>Users list</h1>

<% @users.each do |user|%>
	
	<% if current_user!=user %>
		<%= link_to user.login, new_message_path(:user2_id => user.id) %>(<%= user.name + " " + user.surname %>)<br/>
	<% end %>	
	

<% end %>	