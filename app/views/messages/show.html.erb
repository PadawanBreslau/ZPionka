<%= @message.created_at.strftime("%d.%m.%y %H:%M") %>
<% @user1 = User.find(@message.user1_id) %>
<%= link_to @user1.login , @user1%>
<br/>
<%= @message.message %>
<br/><br/>
<%= link_to "Delete message", @message, :method => :delete, :confirm => "Are you sure?"%> |

<% unless current_user == @user1 %>
	<%= link_to "Respond back", new_message_path(:user2_id => @user1.id) %>
<% end %>



<% if current_user.id == @message.user2_id %>
	<% @message.was_read = true %>
	<% @message.save %>
<% end %>

